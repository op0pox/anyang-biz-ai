# CLAUDE.md — 안양 상권 나침반

2026년 안양시 공공데이터·AI 활용 대학생 경진대회 출품작. 이 파일은 프로젝트의 현재
방향성·설계 결정·제약사항을 정리해, 이 레포에서 작업하는 모든 세션(사람이든 Claude든)이
처음부터 다시 파악하지 않아도 되게 하기 위한 것이다.

## 한 줄 정체성

**행정동×업종 매출 예측 회귀 모델을 기반으로, 안양시청이 소상공인 지원 예산·정책을
어디에 우선 배정할지 판단하게 돕는 행정 의사결정 지원 AI인 동시에, 개별 소상공인
사장님에게 본인이 실제로 받을 수 있는 지원사업을 찾아 신청까지 도와주는 AI.**
"상권분석+매출추정"(소상공인365·오픈업·나이스비즈맵)로는 포지션이 겹쳐 독창성
배점에서 불리하다고 판단해 피해왔지만, "지원사업 매칭·신청 도우미"는 그 서비스들이
다루지 않는 영역이라 개인 창업자향 기능이어도 차별화가 유지된다(2026-09-21 결정,
아래 "지원사업 매칭·신청 도우미" 섹션 참고).

## 핵심 파이프라인: 진단 → 설명 → 처방

지금까지 만든 기능들은 각각 따로 노는 게 아니라, 아래 3단계 파이프라인의 한 자리씩을
맡고 있다. 새 기능을 추가하거나 논의할 때는 항상 "이게 이 파이프라인의 어느 단계에
해당하는가"부터 따질 것 — 그래야 "서비스가 산발적인 기능 모음처럼 보인다"는 문제가
재발하지 않는다. (2026-09-21까지는 ④효과예측(정책 시뮬레이션) 단계가 있었으나 전면
제거함 — 아래 "지금까지 내린 중요한 설계 결정"의 관련 항목 참고.)

```
┌─────────────────┐   ┌─────────────────┐   ┌──────────────────────┐
│ ① 진단 Diagnose  │   │ ② 설명 Explain  │   │ ③ 처방 Prescribe     │
│ "지금 상태가      │ → │ "왜 위험한가"    │ → │ "그래서 뭘 해줘야    │
│  어떤가"          │   │                 │   │  하는가"             │
├─────────────────┤   ├─────────────────┤   ├──────────────────────┤
│ 매출예측 모델      │   │ AI 리포트 생성   │   │ 지원유형 추천         │
│ (RandomForest,   │   │ (LLM+JSON강제,  │   │ (recommend_support_  │
│  log1p)          │   │  근거수치는      │   │  type — 지배요인      │
│ 지원우선순위       │   │  코드가 직접     │   │  기반 정성 진단)      │
│ 스코어링          │   │  렌더링)         │   │        ↓             │
│ (45/30/25 가중합) │   │                 │   │ 3단계 안전망 매칭     │
│                  │   │                 │   │ (희망리턴패키지 등    │
│                  │   │                 │   │  실제 제도명 매핑,    │
│                  │   │                 │   │  match_safety_net)   │
│                  │   │                 │   │        ↓             │
│                  │   │                 │   │ 지원사업 매칭·신청     │
│                  │   │                 │   │ 도우미(사장님 전용,   │
│                  │   │                 │   │  기업마당·K-Startup   │
│                  │   │                 │   │  API, 개인 단위)      │
└────────△─────────┘   └─────────────────┘   └──────────────────────┘
         │
         │ 입력 강화 (안양시 특화 신호 — "안양시 데이터 가점"을 노리는 재료, 별도 기능 아님.
         │ 학습 피처에는 안 넣고 화면에 참고 경고/스탯으로만 노출 — 아래 "설계 결정" 참고)
         ├─ 정비사업 인접 리스크 플래그 — 안양시 자체 발행 데이터, 반경 500m (완료)
         └─ 관측된 폐업 흐름 통계 — 202403↔202606 상가업소번호 비교, 행정동 개편 보정 (완료)

         전달 레이어 (①~③은 완전히 동일, 결과를 "누구에게 어떤 톤으로" 보여줄지만 다름)
         └─ 페르소나 라우팅: 담당자(행정 보고서 톤) / 사장님(개인화된 안내 톤) (완료, app/app.py)
```

**요약**: 우리 프로젝트는 진단→설명→처방의 3단계 파이프라인이고, 정비사업·폐업통계
같은 안양시 특화 신호는 진단 단계의 입력을 강화하는 재료이며, 사장님/담당자 페르소나
라우팅은 이 파이프라인의 결과를 보여주는 방식만 다른 전달 레이어다.

## 타깃 페르소나 2개 (전달 레이어 — 로직이 아니라 UX 라우팅)

| 페르소나 | 니즈 | 담당 페이지(기존 재사용) |
|---|---|---|
| 안양시 경제정책과 / 안양상권활성화센터 담당자 | 관내 소상공인 중 위험군을 조기에 파악해 예산·상담 인력 우선 배정 | 지원우선순위 페이지 — 행정 보고서 톤 유지 |
| 안양시 소상공인 사장님 | 매출이 흔들릴 때 내가 지금 받을 수 있는 지원이 뭔지 빠르게 확인 | 매출예측·업종추천 + AI리포트 — 개인화된 안내 톤, **+ 지원사업 매칭·신청 도우미(신규, `4_지원사업매칭.py`)** |

**설계 결정: 페이지를 두 벌로 완전히 분리하지 않는다.** 위 파이프라인(①~④)은 페르소나와
무관하게 완전히 동일하고, 다른 건 진입점·톤·강조 지표뿐이므로, 홈 화면에 "🏪 사장님이신가요
/ 🏛️ 담당자이신가요" 페르소나 선택 카드를 두고 기존 페이지로 라우팅하는 방식으로 간다.
페이지를 두 벌 만들면 같은 로직을 두 곳에서 유지보수해야 해서 완성도 리스크가 커진다.
(완료 — `app/app.py`의 페르소나 카드)

## 차별화 포인트 3가지 (README·UI·사업계획서에서 일관되게 사용)

1. **안양시 전용 데이터 융합** — 안양시가 직접 발행한 데이터(주민등록인구, 정비사업
   추진현황 등)를 명시적으로 결합
2. **업종 역(逆)추천** — 경쟁 서비스는 사용자가 업종을 직접 골라야 함. 이 서비스는
   지역 기반으로 유리한 업종을 먼저 추천
3. **AI 정책 리포트 자동생성 + 지원사업 매칭·신청 도우미** — 경쟁 서비스는 숫자·그래프만
   제공. 이 서비스는 "왜 우선순위가 높은지"를 설명하고(AI 리포트), 개별 사장님에게는
   실시간 공고 API로 지금 신청 가능한 지원사업까지 찾아줌(2026-09-21, 기존 "정책
   시뮬레이션" 대체 — "지금까지 내린 중요한 설계 결정"의 관련 항목 참고).

## 아키텍처 개요

```
                             [파이프라인 단계]
src/config.py                (공통) 경로, 파일 매핑, 컬럼 힌트, 모델 하이퍼파라미터,
                             우선순위 가중치, 업종 크로스워크 테이블,
                             SAFETY_NET_PROGRAMS, REDEVELOPMENT_RADIUS_M 등
src/data_loader.py           (공통) 데이터셋 로딩. 실데이터마다 스키마가 크게 달라
                             데이터셋별 전용 파싱 로직이 섞여 있음 (아래 "데이터 소스별
                             주의사항" 참고). load_redevelopment_projects(),
                             load_stores_snapshot(quarter): 안양시 특화 신호용 로더.
                             load_support_program_announcements(): 기업마당·K-Startup
                             지원사업 공고 API 연동(③처방, 개인 단위)
src/feature_engineering.py   (①진단 입력) 행정동×업종 피처 테이블 생성.
                             get_redevelopment_risk_by_dong(), compute_closure_rate_table():
                             안양시 특화 신호 — 학습 피처에는 안 넣고 조회 전용
src/model.py                 (①진단 + ③처방)
                               - train_model, predict_sales, compute_support_priority: ①
                               - recommend_support_type(지배 요인 진단), match_safety_net
                                 (스코어 구간별 실제 제도 매칭): ③, 서로 다른 축이라 병렬 사용
src/support_matching.py      (③처방, 개인 단위) match_support_programs — 업종/업력을
                             지원사업 공고와 대조해 정렬 우선순위만 반영(자동 필터링 안 함)
src/ai_report.py             (②설명) LLM 리포트 생성(JSON 구조 강제) + 오프라인 규칙 기반 폴백
app/common.py                캐시 래퍼 모음: load_pipeline, load_redevelopment_risk,
                             load_closure_rate_table, load_support_program_announcements
                             (전부 st.cache_data)
app/app.py + app/pages/      (전달 레이어) Streamlit 멀티페이지(1~4번). app.py에 페르소나
                             라우팅 카드(🏪 사장님 / 🏛️ 담당자) 구현됨
scripts/make_sample_data.py  가상 데이터 생성 (파이프라인 1차 검증용, 지금은 안 씀)
```

`streamlit run app/app.py` 한 줄로 전체 실행. 가상 데이터/실데이터 어느 쪽이든 동일한
코드로 동작하며, `data/raw/`에 실제 파일(또는 심볼릭 링크)이 있으면 그걸 우선 쓴다.

**데이터 폴더 구조** (2026-09-18 정리): 원본 다운로드 파일은 전부 `data/sources/`
아래 데이터셋별 폴더에 있고(`card_sales/`, `stores/202606/`, `stores/202403/`,
`resident_population/`, `redevelopment/`), `data/raw/`의 심볼릭 링크가 여기를 가리킨다.
`stores/202403/`은 최신(202606)과 비교해 폐업 흐름을 추정하기 위한 과거 분기 스냅샷이다.
경진대회 관련 문서(공고문, 제출서류 양식, TAGO API 가이드, 기획서 초안)는 `docs/`에 있다.
**주의**: 한글 파일명을 `bash find -name`/`grep`으로 다룰 때 macOS(NFD)와 터미널 입력(NFC)의
유니코드 정규화가 달라 패턴이 매칭되지 않는 사고가 실제로 있었다(카드소비 데이터 전체가
삭제될 뻔함 — 다행히 환경 안전장치로 복구됨). 한글 파일명이 걸린 삭제/이동은 Python
(`unicodedata.normalize`로 비교) 또는 `pathlib.Path.iterdir()`로 직접 순회할 것 — 절대
셸에 한글 패턴을 직접 타이핑해서 `-delete`/`rm`에 걸지 말 것.

## 지금까지 내린 중요한 설계 결정 (왜 이렇게 했는지 — 다시 논쟁하지 말 것)

- **배포용 데이터는 원본을 그대로 올리지 않고 `data/deploy/`에 안양시 필터링
  축소본을 별도로 둔다**(2026-09-21, `scripts/prepare_deploy_data.py`): 원본
  `data/sources/`는 상가정보 2개 분기(경기도 전체, 각 300~350MB)+카드소비
  3개월치(각 90~110MB)로 총 ~1GB에 달해 GitHub 파일당 100MB 제한을 넘고,
  Streamlit Community Cloud 무료 티어(RAM 1GB)에서 그대로 읽으면 메모리
  초과로 죽을 수 있다. 상가정보는 시군구명에 "안양시"가 들어간 행만
  남기고(실측 4.2%, 350MB→14MB, 스키마는 그대로라 코드 수정 불필요), 카드소비는
  원본이 일별×시간대×성별×연령대까지 쪼개진 130만 행짜리라 필터링만으론
  못 줄여 `_load_card_sales_one_file()`을 재사용해 (행정동,업종) 단위로
  아예 미리 집계해버렸다(1.3M행→250행, 이 출력 스키마는 `data_loader.py`가
  감지해서 그대로 읽는다). 결과 총 30MB 미만. `src/config.py`의
  `_ensure_deploy_data()`가 앱 시작 시 이 파일들을 `data/raw/`·`data/sources/`
  하위로 자동 복사한다 — **단, 로컬 개발 환경처럼 해당 파일(또는 폴더 안 다른
  파일)이 이미 있으면 건너뛴다**(로컬은 전체 데이터 심볼릭 링크를 계속 쓰고,
  배포 환경만 이 축소본을 쓰게 하기 위함). data/raw 전체와 data/sources 전체를
  통째로 옮겨 "새로 클론한 상태"를 재현해 검증했고, 안양시 행만 걸러낸 것이라
  정보 손실 없이 R²·우선순위 랭킹이 원본과 100% 동일하게 나옴을 확인했다.
- **`st.columns()` 같은 행 카드들의 높이를 맞추려면 `app/style.css`에 CSS 체인 전체가
  필요하다**(2026-09-21, Playwright로 실제 렌더링 측정해서 확인): `.anyang-card`에
  `height:100%`만 주는 걸로는 안 된다 — Streamlit이 컬럼과 카드 사이에 끼워 넣는
  `stElementContainer`/`stMarkdown`/`stMarkdownContainer` 등 중간 래퍼들이 block
  요소이거나 flex-column의 메인 축이라 컬럼의 flex stretch(행에서 제일 큰 카드에
  맞추는 것)를 물려받지 못하고 각자 자기 콘텐츠 길이대로만 늘어나, 같은 행인데
  가운데(또는 임의의) 카드만 짧아 보이는 문제가 실제로 있었다(AI리포트 페이지의
  진단/근거해석/정책적 시사점 3카드에서 208px vs 258px로 실측 확인). 중간 래퍼 전부에
  `height:100%`를 연쇄로 줘야 끝까지 전달된다(`app/style.css`의
  `[data-testid="stElementContainer"]` 등 선택자 참고). `st.columns()` 밖의 단독
  카드는 조상이 auto 높이라 이 규칙의 영향을 받지 않는다(안전함, 실측으로 회귀 없음
  확인).
- **타깃 매출값은 `log1p` 변환 후 학습, `expm1`로 복원**: 소수의 대형 점포(백화점 등)가
  나머지보다 한두 자릿수 커서 원본 스케일 학습 시 R²가 음수로 나옴. log 변환 후
  R² 0.28~0.35 수준으로 개선.
- **정책 시뮬레이션(④효과예측 단계) 전면 제거함(2026-09-21)** — `simulate_scenario`,
  `simulate_dong_scenario`, `simulate_priority_shift`, `_pd_effect_ratio`, 부분의존도
  (Partial Dependence) 곡선 계산(`pd_curves`), `SIMULATION_FEATURES`/
  `SIMULATION_GRID_RESOLUTION` 전부 삭제(`src/model.py`, `src/config.py`,
  `1_매출예측_업종추천.py`, `2_지원우선순위.py`). 처음엔 버스정류장 확충 시뮬레이션만
  빼려 했는데(교통행정과 소관이라 담당자도 사장님도 직접 실행할 수 없는 변수), 남은
  유동인구 시뮬레이션도 같은 문제였다 — 유동인구 자체가 구 단위 2개 값이라 슬라이더
  여지가 거의 없었고, 있어도 "유동인구를 늘리는 정책"을 실행할 수 있는 주체가 이
  파이프라인 사용자(담당자/사장님)가 아니었다. "정량적으로 계산 가능하다"가 "실제로
  쓸모 있다"를 보장하지 않는다는 지적을 받고 기능 자체를 없앴다. **`simulation_model`은
  남아있다** — 정책 시뮬레이션이 아니라 예상 범위(`predicted_low`/`predicted_high`)
  산출에 쓰이는 별개 용도라 삭제 대상이 아니다(아래 예측 신뢰도 배지 항목 참고).
- **모델을 2개 학습한다(`model`과 `simulation_model`)**: 기본 예측용 모델
  (min_samples_leaf=2)의 트리별 예측 분산은 표본 크기와 상관없이 요동쳐(최대 50배 차이)
  그대로 신뢰구간처럼 보여주면 오해를 부른다. 예상 범위(`predicted_low`/`predicted_high`)
  산출에는 더 보수적인 모델(min_samples_leaf=10)의 25~75백분위를 대신 쓴다.
- **지원우선순위 스코어 = 45%(매출부진) + 30%(경쟁강도) + 25%(매출효율)**
  (`PRIORITY_WEIGHT_*`, config.py). 근거는 config.py 주석과 UI 접이식 섹션에 이미 서술됨.
  하위 3점수를 항상 UI에 노출한다 — 최종 점수만 보여주면 "아무 데이터나 넣은 거
  아니냐"는 의심을 받았던 실제 피드백이 있었음.
- **AI 리포트는 카드 UI + LLM에 JSON 강제(`response_format`) + 근거 수치는 LLM이
  재서술하지 않고 코드가 직접 렌더링**: 원래 통짜 텍스트였는데 "글이 너무 빽빽해서
  안 읽힌다", "숫자를 AI가 지어낸 것처럼 보인다"는 피드백을 받고 전면 재구성. 각
  근거 수치 옆에 실제 출처(예: "소상공인시장진흥공단 상가정보")를 명시한다.
- **`.env`는 `src/config.py`에서 `load_dotenv()`로 로드된다**: 한때 `python-dotenv`를
  설치만 해두고 아무도 호출하지 않아서 `.env`가 조용히 무시되는 버그가 있었음. 새
  환경변수를 추가할 땐 이 로딩 지점이 이미 있다는 걸 기억할 것.
- **예측 신뢰도 배지는 RandomForest 트리별 예측 분산이 아니라 경쟁점포수(표본 크기
  대리지표)로 판단한다** (`_confidence_level`, `model.py`): 트리별 예측(estimators_)의
  25~75백분위 폭을 신뢰구간으로 써보려 했으나, 매출액 분포가 log1p로도 못 잡을 만큼
  치우쳐 있어 트리 간 분산이 표본 크기와 상관관계가 약했고(-0.1~-0.3 수준), 실측
  최댓값 경쟁점포수(1174개)를 가진 행조차 트리 예측이 최대 50배까지 벌어지는 등
  "표본이 많으면 신뢰도가 높다"는 직관과 안 맞는 경우가 실측으로 확인됐다. 그대로
  사용자에게 보여주면 오히려 신뢰를 깎아먹을 수 있어 기각하고, 직접 설명 가능한
  "관측된 경쟁점포 수"(하위/상위 25% 분위수인 25개/120개를 컷으로) 기준으로 바꿨다.
  **예상 범위(`predicted_low`/`predicted_high`)는 여전히 트리 분산을 쓰지만**, 정확도용
  모델(`model`, min_samples_leaf=2) 대신 더 보수적인 모델(`simulation_model`,
  min_samples_leaf=10)의 25~75백분위를 쓴다 — 같은 트리 잡음 문제라 같은 해법(보수적
  모델)을 재사용했다. (`simulation_model`이라는 이름은 2026-09-21에 제거된 정책
  시뮬레이션 기능에서 유래했지만, 지금은 이 예상 범위 산출이 유일한 용도다.)
- **Streamlit 기본 UI(Deploy/메뉴/실행상태) 숨김은 `[data-testid="stHeader"]`를 통째로
  `display: none` 하면 안 된다**(`app/style.css`): 사이드바를 접었을 때 "다시 펼치기"
  화살표(`stExpandSidebarButton`)는 접힌 사이드바 안이 아니라 이 header 툴바 안에서
  렌더링된다 — header를 완전히 숨기면 사이드바를 한 번 접은 뒤 다시 펼칠 방법이 사라지는
  실사용 버그가 났다(사용자가 직접 겪고 리포트함). 올바른 방법: header는 유지하되
  `background: transparent` + `height/min-height: auto/0`으로 바꾸고, 그 안의
  `stAppDeployButton`/`stMainMenu`/`stStatusWidget`(Deploy·메뉴·Stop) 개별 요소만
  `display: none` 한다 — 평소엔 빈 줄로 안 보이다가 펼치기 버튼이 필요할 때만 나타난다.
- **조회 조건(행정동/업종 선택) 위젯은 사이드바가 아니라 결과 바로 위 메인 화면에
  둔다**: 매출예측·업종추천/AI리포트 페이지가 처음엔 `st.sidebar`에 이 위젯들을 뒀는데,
  "사이드바에서 조건을 검색할 수 있다는 느낌이 안 든다"는 실제 피드백을 받고 메인
  화면 상단 "🔍 조회 조건" 카드(`st.container(border=True)`)로 옮겼다. 지원우선순위
  페이지는 처음부터 메인 화면에 `st.selectbox`를 썼던 것과도 일관됨. 새 조회/필터
  위젯을 추가할 때 이 패턴을 따를 것 — 사이드바는 페이지 이동 내비게이션 전용으로 둔다.

## 데이터 소스별 주의사항 (실데이터 연동 중 실제로 겪은 문제들)

| 데이터셋 | 상태 | 주의사항 |
|---|---|---|
| `card_sales` | 실데이터 (2026 1~3월, 월별 3파일) | 컬럼이 코드화(`admi_cty_no`, `card_tpbuz_nm_1`, `amt`, `cnt`)돼 있어 키워드 매칭 불가 → 전용 파서. 행정동 이름이 없고 코드뿐이라 `stores`의 행정동코드로 매핑해야 함(따라서 `stores`가 먼저 있어야 함). **⚠️ 미해결 버그(2026-09-18 발견, 수정 보류)**: `dong_ref`를 최신(202606) `stores`로만 만들어서, 카드소비 데이터가 쓰는 개편 전 행정동코드(41171580=안양8동, 41171581=안양9동, 202403 스냅샷으로 확인)가 조인에서 전부 탈락함(`data_loader.py:300`) → **명학동·병목안동은 전 업종 카드매출이 0으로 학습되고 있음**(경쟁점포는 각각 358개/263개 있는데도). 이 버그 때문에 두 동이 지원우선순위 랭킹 top 3에 인위적으로 올라와 있을 가능성이 높음(매출부진 가중치 45%). 고칠 때는 202403 스냅샷의 안양8동(580)+안양9동(581) 합산 실제 매출을, 현재 경쟁점포수 비율로 명학동/병목안동에 안분하는 방식을 제안함(`resident_population`의 구→동 배분과 같은 패턴, 추정치 캡션 필수). 박달동·부흥동의 "미디어/통신" 업종도 개별적으로 매출 0(경쟁점포 32개·124개)인 게 관측됐는데 이건 같은 행정동코드 원인인지 미확인 — 별도 확인 필요 |
| `stores` | 실데이터 (경기도 전체, 안양시로 필터링) | 컬럼 힌트 순서 주의 — "행정동" 키워드가 "행정동코드"에도 매칭돼서 이름 대신 코드를 잘못 집던 버그 있었음. 반드시 "행정동명" 같은 구체적 키워드를 먼저 시도 |
| `resident_population` | 실데이터 (안양시 발행 xlsx) | 행정동 단위 표가 아니라 "안양시/-만안구/-동안구" 구 단위 병합 셀 피벗 테이블. 전용 파서로 구 총인구를 그 구의 모든 행정동에 동일 배분(유동인구와 같은 한계) |
| `bus_stops` | 실데이터 (TAGO API) | `BUS_API_KEY` 필요(.env). **cityCode로 조회해도 인접 시(광명·군포·의왕·부천·서울) 정류소가 다수 섞여 나옴** — 상가정보의 실제 위경도 bounding box로 재필터링해서 완화(1214건→761건, 완전히 제거되진 않음, 알려진 한계) |
| `floating_population` | 실데이터 (경기데이터드림 API, 서비스명 `TBDASANALSGALLERYT214117`) | 응답이 구×시간대×성별×5세연령대×내/외국인으로 잘게 쪼개진 **21만여 건의 다년간 누적 통계**라 전체를 안 받고 대표 표본(`GG_OPEN_API_MAX_PAGES`, 기본 2만 건)만 집계. **실질적으로 만안구/동안구 2개 값만 존재** — 행정동 단위 아님 |

## 안양시 특화 신호 확장 (2026-09-18 논의 → 구현·검증 완료)

사용자가 별도로 "폐업위험 조기경보 & 3단계 안전망 매칭 서비스" 기획서를 제시했고,
검토 끝에 **전면 재구축은 하지 않고, 실행 가능한 일부만 기존 파이프라인에 이식**하기로
결정했다. 이유: 진짜 폐업 예측(생존분석)에 필요한 점포별 폐업 이력 라벨이 없었음.

논의 중 새로 확인된 사실 (중요):
- **정비사업 재건축·리모델링 추진현황 데이터**([data.go.kr/data/15150142](https://www.data.go.kr/data/15150142/fileData.do))는
  **안양시가 직접 발행**(제공기관: 경기도 안양시 AI정책과) — 위도/경도, 착공일자·준공일자·
  조합설립인가일자, 세대수 필드 보유, 42건. 공공데이터 가점에도 유리. → "정비사업 인접
  리스크 플래그" 피처로 채택 확정.
- **100대 생활업종 신규사업자 현황**([data.go.kr/data/15048948](https://www.data.go.kr/data/15048948/fileData.do))는
  기대와 다름 — 이름 그대로 "신규사업자"(개업) 집계 통계고, 국세청이 연 1회 발행하는
  전국 단위 100행짜리 표. 폐업 데이터 아니고 안양시 필터링도 안 됨. **폐업 라벨 문제를
  해결해주지 않는다** — 채택 안 함.
- **상가(상권)정보([data.go.kr/data/15083033](https://www.data.go.kr/data/15083033/fileData.do))에는
  "주기성 과거 데이터" 탭에 2024년 1분기(202403)~2026년 2분기(202606) 최소 9개 분기의
  과거 스냅샷이 실제로 존재함**(이 탭은 JS 렌더링이라 일반 fetch로는 안 보이고 브라우저로
  직접 확인해야 함). 두 시점을 비교해 "이전엔 있었는데 사라진 상가업소번호 = 폐업 추정"하는
  라벨링이 가능. `상가업소번호` 컬럼이 분기 간 안정적인 ID로 보여서, 상호명/주소 퍼지매칭 없이
  ID 집합 비교만으로 가능할 것으로 판단(아직 실제로 검증 안 함).

**결정한 스코프** (완성도 리스크 때문에 의도적으로 축소 — 파이프라인 단계 표기는 위
"핵심 파이프라인" 다이어그램 참고). **아래 1~3, 5번은 2026-09-18에 전부 구현·검증
완료했다** (4번만 이번 대회 스코프 아웃):

1. ✅ **완료**: **3단계 안전망 매칭** — `src/model.py`의 `match_safety_net(priority_score)`.
   우선순위 스코어의 절대 수준(0~100)을 예방/긴급수혈/재기지원 3구간으로 나눠
   `config.py`의 `SAFETY_NET_PROGRAMS`(희망리턴패키지, 안양시 소상공인 특례보증,
   중소기업 육성자금, 안양상권활성화센터 컨설팅)를 매칭한다. 기존
   `recommend_support_type()`(지배 요인=왜 필요한지)과는 **다른 축**(=얼마나 급한지)이라
   나란히 보여준다. UI: `2_지원우선순위.py`.
2. ✅ **완료**: **정비사업 인접 리스크 플래그** — `src/data_loader.py`의
   `load_redevelopment_projects()`(완료된 사업은 제외) + `src/feature_engineering.py`의
   `get_redevelopment_risk_by_dong()`(반경 내 haversine 조회). **반경은 실측으로 정함**:
   800m는 31개 동 중 18개가 걸려 신호가 희석되어 500m로 좁힘(11개 동만 해당). 학습
   피처에는 넣지 않고 AI리포트·우선순위 페이지에 조건부 경고 카드로만 노출.
3. ✅ **완료**: **관측된 폐업 흐름 통계** — `src/data_loader.py`의
   `load_stores_snapshot(quarter)` + `src/feature_engineering.py`의
   `compute_closure_rate_table()`. `상가업소번호`가 분기 간 안정적인 ID임을 샘플
   검증(20건 전부 상호명 일치)한 뒤 구현. **구현 중 실제 버그를 잡았다**: 202403→202606
   사이 행정동 개편(박달1동+박달2동→박달동 통합, 안양8동+안양9동→명학동/병목안동/
   호현동 개편)이 있어서, 이걸 감안 안 하면 그 4개 동이 "폐업률 100%"로 완전히 잘못
   나왔다(실제로는 동 이름이 바뀐 것뿐). 두 분기 모두에 존재하는 동만 비교하도록
   고쳐서 해결(`compute_closure_rate_table`의 `common_dongs` 필터). 학습 피처에는
   넣지 않고 AI리포트의 5번째 근거 스탯("최근 폐업 후보 비율", 추정치임을 명시)으로 노출.
4. ❌ 스코프 아웃(이번 대회에서는 구현 안 함): 개별 점포 단위 폐업 확률을 학습된
   분류/생존모델로 예측. 사업계획서에 "향후 확장 방향"으로만 서술.
5. ✅ **완료**: 홈 화면 페르소나 라우팅 — `app/app.py`에 "🏪 사장님 / 🏛️ 담당자" 카드 2개
   + 각 카드 아래 해당 페이지로의 `page_link`. 기존 3개 개별 링크는 접이식
   "또는 원하는 페이지로 바로 이동" 안으로 축소.

이 4가지 작업의 상세 설계는 `/Users/eega0/.claude/plans/tender-exploring-avalanche.md`에
남아있다(승인된 계획, Context/스코프 결정 근거 포함).

## 지원사업 매칭·신청 도우미 (2026-09-21 결정 → API 연동 완료, `feature/support-program-matching` 브랜치)

사용자가 "공무원 페이지의 참고 카드들은 정보만 주지 실제 행동으로 안 이어진다"는
문제를 제기했고, 논의 끝에 사장님 페르소나에 **③처방 단계를 개인 단위로 확장**하는
기능을 추가하기로 했다 — 행정동×업종 집계가 아니라 "이 사장님 한 명"이 실제로 받을
수 있는 지원사업을 찾아 신청까지 도와주는 기능. 기존 파이프라인을 대체하는 게
아니라 사장님 전용 페이지 하나를 추가하는 것이라 "페이지를 두 벌로 분리하지 않는다"
원칙과 충돌하지 않는다(담당자에게는 의미 없는 개인화 기능이라 원래도 공유 대상이
아니었음).

**스코프 결정 (원안 대비 축소)**:
- 공고문 실시간 자동 수집은 한다 — 단, HTML 스크래핑이 아니라 **기업마당(bizinfo.go.kr)
  지원사업정보 API + K-Startup 통합공고 API**(둘 다 data.go.kr 정식 Open API) 연동으로
  한다. 기존 TAGO/경기데이터드림 연동과 같은 패턴(서비스키 발급 → 페이지네이션 수집
  → 캐시)이라 아키텍처가 늘어나지 않는다.
- ✅ **API 연동 완료(2026-09-21)** — `src/data_loader.py`의 `_fetch_bizinfo_announcements()`
  / `_fetch_kstartup_announcements()`. 기업마당은 `hashtags="안양"` 서버측 검색으로
  안양시 관련 공고만 바로 받고(`crtfcKey` 파라미터), K-Startup은 지역 검색 파라미터를
  못 찾아 최근 공고 여러 페이지를 받아 "모집중(`rcrt_prgs_yn=Y`) + 안양/경기 주소·기관명
  또는 전국 대상"만 클라이언트에서 남긴다(`KSTARTUP_MAX_PAGES`, config.py). K-Startup은
  페이지 간 응답이 일부 겹쳐 `pbanc_sn`(공고 일련번호) 기준으로 중복 제거 필요했음.
  키 없으면 bus_stops/floating_population과 같은 패턴으로 구조검증용 가상 데이터로
  폴백한다(`_make_fallback_support_programs`).
- ⚠️ **실측으로 발견한 설계 오류**: 처음엔 공고의 `category` 필드(기업마당
  `pldirSportRealmLclasCodeNm`, K-Startup `supt_biz_clsfc`)를 우리 시스템의 업종
  체계(음식/소매유통 등, `BUSINESS_CATEGORIES`)와 매칭해 필터링하려 했으나, 실제
  값을 받아보니 이 필드는 "업종"이 아니라 "경영/금융/수출/사업화/시설공간보육" 같은
  **지원 유형** 분류였다 — 9개 업종 전부로 매칭을 시도한 결과 전부 0건이 나와서
  발견함. 특례보증·경영컨설팅 같은 지원사업 대다수가 애초에 업종을 가리지 않으므로,
  이 필드로 업종 필터링을 하는 것 자체가 데이터 오독이었다. **수정**: 업종으로
  걸러내지 않고 마감 여부만 거르며, target/exclude 텍스트에 업종 키워드가 직접
  언급된 공고만 정렬 우선순위를 높인다(`src/support_matching.py`의
  `match_support_programs`). 매출·업력·고용인원도 같은 이유(표기 형식이 공고마다
  제각각)로 자동 필터링에 안 쓰고 참고 정보로만 노출한다.
- LLM이 공고문 제외대상·서류요건을 잘못 읽어 "신청 가능하다고 나왔는데 탈락했다"가
  되는 리스크는, AI 리포트와 같은 원칙(근거는 코드가 렌더링, LLM은 재서술만)에 더해
  **모든 매칭 결과에 원문 공고 링크 + "AI 추정이니 반드시 원문에서 최종 확인" 안내문을
  같이 붙이는 것으로 완화**하기로 함(사용자 결정, 2026-09-21). 최종 신청 가능 여부
  판단은 사용자 책임으로 넘기되, 확인을 쉽게 만드는 것까지가 이 기능의 책임. (현재
  매칭 로직 자체가 규칙 기반이라 LLM을 아직 안 썼다 — K-Startup의 `aply_excl_trgt_ctnt`
  제외대상 원문은 그대로 노출만 하고 AI가 재해석하지 않는다.)
- ❌ 스코프 아웃: 카카오톡 알림톡 마감일 알림(사업자 인증·템플릿 사전 심사가 필요해
  대회 기간 내 불가능), 사업자 단위 중복지원·효과분석 대시보드(사업자별 지원이력은
  비공개 데이터라 공공데이터로 구할 수 없음). 둘 다 사업계획서에 "향후 확장 방향"으로만
  서술.

## 실행/검증 방법 (매번 지킬 것)

- 코드 변경 후: `./venv-anyang/bin/python -m py_compile <파일>` → `streamlit.testing.v1.AppTest`로
  예외 유무 확인 → 필요시 Playwright로 스크린샷까지 확인. 이 3단계를 항상 거쳤고, 실제로
  AppTest에서만 잡히던 크래시(슬라이더 min==max 등)가 여러 번 있었다.
- 백그라운드 API 호출(버스정류소, 유동인구)은 페이지네이션 때문에 시간이 좀 걸린다 —
  `run_in_background: true`로 돌리고 완료 알림을 기다릴 것.
- 실데이터/가상데이터 양쪽 다 회귀 테스트할 것 — 실데이터용으로 고친 로직(예: 업종
  크로스워크, 구 단위 배분)이 가상데이터 경로를 조용히 깨뜨린 적이 두 번 있었다.
